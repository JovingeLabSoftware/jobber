from setuptools import setup

setup(name='jpbber',
      version='0.1',
      description='Bulk loader with throttling for HPC/Torque',
      url='',
      author='Eric Kort',
      author_email='eric.kort@vai.org',
      license='MIT',
      packages=['jobber'],
      install_requires=[],
      zip_safe=False)